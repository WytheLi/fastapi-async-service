"""
Internal Code

用于系统内部错误或状态追踪.
### 模块命名建议：
1. **模块划分**：根据业务功能划分模块，比如用户模块、订单模块、支付模块等。
2. **模块编码**：为每个模块分配一个唯一的两位数字编码（00~99），例如：
   - 00：系统通用模块（如数据库错误、网络错误等）
   - 10：用户模块
   - 20：订单模块
   - 30：支付模块
   - ... 以此类推
3. **子模块编码**：每个模块内部可以再分子模块，用两位数字表示（00~99）。例如在用户模块下：
   - 01：登录
   - 02：注册
   - 03：用户信息管理
   - ...
4. **具体错误码**：每个子模块下的具体错误用两位数字表示（00~99）。例如在登录子模块下：
   - 01：用户名不存在
   - 02：密码错误
   - 03：账户被锁定
   - ...
### 示例：
假设用户模块编码为10，登录子模块为01，那么：
- 100101：用户名不存在
- 100102：密码错误
"""

SUCCESS = 200
USER_IS_DISABLE = 100101  # 用户被禁用
INVALID_ENCRYPTED_REQUEST = 100102  # 无效的加密请求
